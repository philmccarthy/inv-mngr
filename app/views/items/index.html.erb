<h1>Items in Inventory</h1>

<section id="items">
<%= button_to 'New Item', new_item_path %>
<ul>
  <% @items.each do |item| %>
    <section id="item-<%= item.id %>">
      <li><%= render partial: 'details', locals: { item: item } %></li>
    </section>
  <% end %>
</ul>
</section>
